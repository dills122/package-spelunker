# Applications

Thin process and transport adapters belong here. Planned applications are `cli` and `mcp-server`.
They validate inputs, invoke package APIs, map errors, manage lifecycle, and format results; domain
analysis does not belong in an application package.
